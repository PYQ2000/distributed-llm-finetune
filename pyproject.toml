[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "distributed-llm-finetune"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["dft"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
